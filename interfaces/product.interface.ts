// Define the Product interface based on the data structure
export interface Product {
    quantity: any;
    categoryId: number;
    id: number;
    image: string;
    name: string;
    price: number;
}
